import {
  header: {
    container,
    text
  }
} from components_bss;

module exports

default `
 <div class={container()}>
    <h1>
      <a class={
        text({ type: "link" })
      } href="/">
        ブログ By Bord
      </a>
    <h1>
  </div>
`